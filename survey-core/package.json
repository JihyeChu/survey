{
  "name": "survey-core",
  "version": "1.0.0",
  "description": "Independent survey builder and responder module",
  "main": "dist/survey-core.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/test/**/*.test.js"],
    "collectCoverageFrom": ["src/**/*.js"],
    "verbose": true
  },
  "keywords": ["survey", "form", "builder", "google-form"],
  "license": "MIT"
}
